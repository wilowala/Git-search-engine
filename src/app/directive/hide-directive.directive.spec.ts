import { HideDirectiveDirective } from './hide-directive.directive';

describe('HideDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HideDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
