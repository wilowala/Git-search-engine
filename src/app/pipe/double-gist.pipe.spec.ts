import { DoubleGistPipe } from './double-gist.pipe';

describe('DoubleGistPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleGistPipe();
    expect(pipe).toBeTruthy();
  });
});
